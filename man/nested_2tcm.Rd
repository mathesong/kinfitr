% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinfitr_nested_2tcm.R
\name{nested_2tcm}
\alias{nested_2tcm}
\title{Nested 2TCM with Shared Macroparameters}
\usage{
nested_2tcm(
  t_tac,
  tac,
  region,
  input,
  shared = c("Vnd", "k4", "Vnd_k4"),
  inpshift = 0,
  vB = 0.05,
  weights = NULL,
  roiweights = NULL,
  Vnd.start = 1,
  Vnd.lower = 1e-04,
  Vnd.upper = 10,
  k4.start = 0.1,
  k4.lower = 1e-04,
  k4.upper = 0.5,
  K1.start = 0.1,
  K1.lower = 1e-04,
  K1.upper = 1,
  BPp.start = 1,
  BPp.lower = 1e-04,
  BPp.upper = 50,
  optim_method = c("L-BFGS-B", "Nelder-Mead", "BFGS", "CG", "SANN"),
  optim_control = list(),
  multstart_iter = 1,
  multstart_lower = NULL,
  multstart_upper = NULL,
  frameStartEnd = NULL,
  timeStartEnd = NULL,
  keep_inner_fits = FALSE
)
}
\arguments{
\item{t_tac}{Numeric vector of times (minutes), repeated per region.}

\item{tac}{Numeric vector of radioactivity, stacked per region.}

\item{region}{Character vector identifying regions.}

\item{input}{Data frame from blood_interp().}

\item{shared}{Character. Which parameters to share across regions in the
outer loop. One of \code{"Vnd"}, \code{"k4"}, or \code{"Vnd_k4"}.}

\item{inpshift}{The delay in minutes. Must be pre-fitted (not estimated
here). Default is 0.}

\item{vB}{Blood volume fraction. Default is 0.05.}

\item{weights}{Optional. Numeric vector of weights.}

\item{roiweights}{Optional. Numeric vector of ROI weights. Can be a named
vector (one per region), an unnamed vector of the same length as the
number of regions, or a long vector the same length as \code{tac}
(one per observation; the first value per region is used). ROI size or
relative size is a useful quantity to supply here, as larger regions
provide more precise mean TAC estimates. Normalised internally so the
maximum is 1.}

\item{Vnd.start}{Starting value for Vnd. Default is 1.}

\item{Vnd.lower}{Lower bound for Vnd. Default is 0.0001.}

\item{Vnd.upper}{Upper bound for Vnd. Default is 10.}

\item{k4.start}{Starting value for k4. Default is 0.1.}

\item{k4.lower}{Lower bound for k4. Default is 0.0001.}

\item{k4.upper}{Upper bound for k4. Default is 0.5.}

\item{K1.start}{Starting parameter for K1. Default is 0.1.}

\item{K1.lower}{Lower bound for K1. Default is 0.0001.}

\item{K1.upper}{Upper bound for K1. Default is 1.}

\item{BPp.start}{Starting parameter for BPp. Default is 1.}

\item{BPp.lower}{Lower bound for BPp. Default is 0.0001.}

\item{BPp.upper}{Upper bound for BPp. Default is 50.}

\item{optim_method}{Optimization method. Default is "L-BFGS-B".
Only "L-BFGS-B" supports bounds on the shared (outer) parameters; if
another method is selected, the lower and upper limits for shared
parameters are ignored.}

\item{optim_control}{Control parameters for optim().}

\item{multstart_iter}{Multistart iterations for inner fits. Default is 1.}

\item{multstart_lower}{Optional. Lower bounds for multistart.}

\item{multstart_upper}{Optional. Upper bounds for multistart.}

\item{frameStartEnd}{Optional. Frame range c(start, end).}

\item{timeStartEnd}{Optional. Time range for frame selection.}

\item{keep_inner_fits}{Logical. If TRUE, store per-region nlsLM fit objects
in \code{$fits_inner}. Default is FALSE.}
}
\value{
A list with class c("nested_2tcm", "kinfit").
}
\description{
Fits the Two Tissue Compartment Model (macro parameterisation) to multiple
regions simultaneously. Optimizes shared parameters (Vnd and/or k4) across
all regions using optim() in the outer loop, while fitting per-region
parameters using nlsLM in the inner loop. Uses
\code{twotcm_macro_model(t_tac, input, K1, Vnd, BPp, k4, vB)}.
}
\details{
The \code{shared} argument controls which parameters are estimated jointly
across regions in the outer loop:
\itemize{
  \item \code{"Vnd"}: Vnd shared across regions. Inner fits K1, BPp, k4
    per region.
  \item \code{"k4"}: k4 shared across regions. Inner fits K1, Vnd, BPp
    per region.
  \item \code{"Vnd_k4"}: Both Vnd and k4 shared. Inner fits K1, BPp
    per region.
}
}
