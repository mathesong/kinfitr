% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinfitr_nested_srtm.R
\name{nested_srtm}
\alias{nested_srtm}
\title{Nested SRTM with Shared k2prime Estimation}
\usage{
nested_srtm(
  t_tac,
  roitac,
  reftac,
  region,
  weights = NULL,
  roiweights = NULL,
  k2prime.start = 0.1,
  k2prime.lower = 0.001,
  k2prime.upper = 1,
  R1.start = 1,
  R1.lower = 0,
  R1.upper = 10,
  bp.start = 1.5,
  bp.lower = 0,
  bp.upper = 15,
  optim_method = c("L-BFGS-B", "Nelder-Mead", "BFGS", "CG", "SANN"),
  optim_control = list(),
  multstart_iter = 1,
  multstart_lower = NULL,
  multstart_upper = NULL,
  frameStartEnd = NULL,
  timeStartEnd = NULL,
  keep_inner_fits = FALSE
)
}
\arguments{
\item{t_tac}{Numeric vector of times (minutes), repeated per region.}

\item{roitac}{Numeric vector of target ROI radioactivity, stacked per region.}

\item{reftac}{Numeric vector of reference tissue radioactivity. Should be the
same reference region values repeated for each target region (i.e., same
length as roitac). Alternatively, can be a single vector of per-frame
values that will be repeated for each region.}

\item{region}{Character vector identifying the region for each observation.}

\item{weights}{Optional. Numeric vector of weights.}

\item{roiweights}{Optional. Numeric vector of ROI weights. Can be a named
vector (one per region), an unnamed vector of the same length as the
number of regions, or a long vector the same length as \code{roitac}
(one per observation; the first value per region is used). ROI size or
relative size is a useful quantity to supply here, as larger regions
provide more precise mean TAC estimates. Normalised internally so the
maximum is 1.}

\item{k2prime.start}{Starting value for k2prime. Default is 0.1.}

\item{k2prime.lower}{Lower bound for k2prime. Default is 0.001.}

\item{k2prime.upper}{Upper bound for k2prime. Default is 1.}

\item{R1.start}{Starting parameter for R1. Default is 1.}

\item{R1.lower}{Lower bound for R1. Default is 0.}

\item{R1.upper}{Upper bound for R1. Default is 10.}

\item{bp.start}{Starting parameter for bp. Default is 1.5.}

\item{bp.lower}{Lower bound for bp. Default is 0.}

\item{bp.upper}{Upper bound for bp. Default is 15.}

\item{optim_method}{Optimization method. Default is "L-BFGS-B".
Only "L-BFGS-B" supports bounds on the shared (outer) parameters; if
another method is selected, the lower and upper limits for shared
parameters are ignored.}

\item{optim_control}{Control parameters for optim().}

\item{multstart_iter}{Multistart iterations for inner fits. Default is 1.}

\item{multstart_lower}{Optional. Lower bounds for multistart.}

\item{multstart_upper}{Optional. Upper bounds for multistart.}

\item{frameStartEnd}{Optional. Frame range c(start, end).}

\item{timeStartEnd}{Optional. Time range for frame selection.}

\item{keep_inner_fits}{Logical. If TRUE, store per-region nlsLM fit objects
in \code{$fits_inner}. Default is FALSE.}
}
\value{
A list with class c("nested_srtm", "kinfit").
}
\description{
Fits the Simplified Reference Tissue Model 2 (SRTM2) to multiple regions
simultaneously, estimating a shared k2prime across all regions using optim()
in the outer loop, while fitting R1 and bp per region using nlsLM in the
inner loop. This is the proper way to estimate k2prime for SRTM2 — by
leveraging information across multiple regions.
}
